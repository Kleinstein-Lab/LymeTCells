# Lyme T Cells

The code in this repository is made available in the form of R Markdown (RMD) files. If you prefer R file, you can convert a RMD notebook to an R script with `knitr::purl()`. This function includes an argument called `documentation` for setting whether or not you'd like to keep any additional text from the RMD notebook.

## Structure
